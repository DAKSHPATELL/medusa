{
  "name": "@clearborder/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts for ClearBorder: AgentEvent stream, core models, wire protocol, portal domain.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "tsx --test src/temporal/timeline.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
